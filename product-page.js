const id=new URLSearchParams(location.search).get('id')||'candle-filter',p=getProduct(id)||PRODUCTS[0];document.title=`${p.name} | بهریز مبدل`;
document.querySelector('#product-page').innerHTML=`<section class="product-hero"><div class="container product-hero-grid"><div class="product-intro"><a href="products.html">محصولات / ${p.cat}</a><span>${p.en}</span><h1>${p.name}</h1><p>${p.summary}</p><a class="btn btn-primary" href="index.html#contact">درخواست بررسی فنی ←</a></div><div class="product-visual"><img src="${p.image}" alt="${p.name}"><i>BEHRIZ / ${p.cat}</i></div></div></section><section class="product-content"><div class="container product-content-grid"><div><span class="eyebrow burgundy"><i></i> کاربرد</span><h2>کاربرد دستگاه</h2><p>${p.application}</p><span class="eyebrow burgundy"><i></i> مزایا</span><h2>مزیت‌های محصول</h2><ul class="benefit-list">${p.benefits.map(x=>`<li>${x}</li>`).join('')}</ul></div><aside><span>TECHNICAL DATA</span><h2>مشخصات فنی</h2><dl>${p.specs.map(x=>`<div><dt>${x[0]}</dt><dd>${x[1]}</dd></div>`).join('')}</dl><small>مشخصات نهایی براساس محصول، ظرفیت و شرایط خط تولید تعیین می‌شود.</small></aside></div></section><section class="related"><div class="container"><h2>محصولات مرتبط</h2><div class="related-grid">${PRODUCTS.filter(x=>x.cat===p.cat&&x.id!==p.id).slice(0,3).map(x=>`<a href="product.html?id=${x.id}"><img src="${x.image}" alt="${x.name}"><b>${x.name}</b><span>مشاهده ←</span></a>`).join('')}</div></div></section>`;
